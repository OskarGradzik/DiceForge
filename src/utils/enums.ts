export enum UserComponent {
  Undefined = 'undefined',
  Character = 'character',
  Notes = 'notes',
  Settings = 'settings',
  ManualPage = 'guide',
}

export enum AlertTypes {
  Error = 'error',
  Info = 'info',
  Warning = 'warning',
}

export enum NoteTypes {
  Important = 'important',
  Secondary = 'secondary',
}
